process testJET = {

   include "FWCore/MessageService/data/MessageLogger.cfi"
   replace MessageLogger.cout.threshold = "DEBUG"
   replace MessageLogger.cerr.default.limit = 10

   include "SimGeneral/HepPDTESSource/data/pythiapdt.cfi"

   untracked PSet maxEvents = {untracked int32 input = -1}

# OK for running at CERN
  source = PoolSource {
    untracked vstring fileNames = {
        "file:/afs/cern.ch/user/s/santocch/public/ttbar_inc_0.root"
     }
  }

  module printTree = ParticleTreeDrawer {
    InputTag src = genParticleCandidates
    untracked bool printP4 = false
    untracked bool printPtEtaPhi = true
    untracked bool printVertex = false    
    untracked bool printStatus = false
    untracked bool printIndex = false
    untracked vint32 status = { 3 }
  }  

  module genJetCollectionClone = GenJetShallowCloneProducer{
     InputTag src = iterativeCone5GenJets
  }

   module genJetSele = PtMinCandSelector{
     InputTag src = genJetCollectionClone
     double ptMin        = 20.0
  }

# the anlyzer to print generated Event and GenJet infos
  module printEvent = printGenEvent {
    InputTag src = genJetSele
  }

  module printEventNumber = AsciiOutputModule { }
  path p = {printTree,genJetCollectionClone,genJetSele, printEvent }

# Now order modules for execution
  endpath outpath = {printEventNumber}

}

